# LaX

Portable local stack: Apache / Nginx / MariaDB / PHP. Tauri desktop app.

## Run

Copy the contents of a release zip to a folder and start `lax.exe`.

## Dev

```
npm install
npm run build:exe
npm run pack
```
